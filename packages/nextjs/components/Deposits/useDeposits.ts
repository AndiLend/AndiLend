const useDeposits = () => {
  const scoreData = 100;
  const fundingReceived = "108.00";
  const interest = "5.1";
  const totalPayment = "1,078";

  return { fundingReceived, interest, totalPayment, scoreData };
};

export default useDeposits;
